<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <androidx.core.widget.NestedScrollView
        android:id="@+id/font_info_scroll_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:id="@+id/font_info_items_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="@dimen/sesl_dialog_padding_horizontal"
            android:paddingEnd="@dimen/sesl_dialog_padding_horizontal"
            android:paddingTop="@dimen/sesl_dialog_title_padding_bottom"
            android:paddingBottom="@dimen/sesl_dialog_body_text_padding_bottom" />

    </androidx.core.widget.NestedScrollView>

    <View
        android:id="@+id/font_info_scroll_indicator_up"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_gravity="top"
        android:background="?attr/colorControlHighlight"
        android:visibility="gone" />

    <View
        android:id="@+id/font_info_scroll_indicator_down"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_gravity="bottom"
        android:background="?attr/colorControlHighlight"
        android:visibility="gone" />

</FrameLayout>
