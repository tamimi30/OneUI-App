<?xml version="1.0" encoding="utf-8"?>
<dev.oneuiproject.oneui.layout.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:expanded="false"
    app:title="@string/app_name"
    app:subtitle="@string/app_subtitle">

    <FrameLayout
        android:id="@+id/main_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_location="main_content" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/drawer_list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:overScrollMode="always"
        android:scrollbars="vertical"
        app:layout_location="drawer_panel" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_font_size"
        android:theme="@style/Theme.MaterialComponents.DayNight"
        style="@style/FabStyle"
        android:transitionName="fabTransition"
        android:visibility="gone"
        android:tooltipText="@string/font_size_dialog_title"
        app:layout_location="root" />

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/format_bar_card"
        android:theme="@style/Theme.MaterialComponents.DayNight"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|start"
        android:layout_margin="24dp"
        app:cardBackgroundColor="@color/sesl_menu_popup_bg_color"
        android:visibility="gone"
        app:cardCornerRadius="26dp"
        app:cardElevation="4dp"
        
        app:layout_location="root">
        	
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="10dp"
            android:orientation="horizontal">
            

            <ImageButton
                android:id="@+id/btn_bold"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginEnd="10dp"
                android:background="@drawable/bg_format_toggle"
                android:src="@drawable/ic_oui_text_bold"
                android:tooltipText="@string/btn_bold"
                android:gravity="center"/>
                
            
            
         
            <ImageButton
                android:id="@+id/btn_italic"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/bg_format_toggle"
                android:src="@drawable/ic_oui_text_italic"
                android:tooltipText="@string/btn_italic"
                android:gravity="center" />

        
        
        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

</dev.oneuiproject.oneui.layout.DrawerLayout>
